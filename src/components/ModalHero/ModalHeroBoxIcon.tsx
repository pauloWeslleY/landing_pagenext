import { ReactNode } from 'react'
import { ModalBoxIcon } from './styles'

export const ModalHeroBoxIcon = ({ children }: { children: ReactNode }) => (
  <ModalBoxIcon>{children}</ModalBoxIcon>
)
