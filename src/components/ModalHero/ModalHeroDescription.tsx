import { ReactNode } from 'react'
import { ModalDescription } from './styles'

export const ModalHeroDescription = ({ children }: { children: ReactNode }) => (
  <ModalDescription>{children}</ModalDescription>
)
